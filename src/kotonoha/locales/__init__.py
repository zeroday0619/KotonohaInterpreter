"""Message catalogs. `en` is the reference; the others must carry the same keys.

`tests/test_i18n.py` enforces key parity, so a string added to English without a
translation fails the test suite rather than silently falling back at runtime.
"""
